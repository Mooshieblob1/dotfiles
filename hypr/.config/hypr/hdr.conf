# DP-2 HDR runtime state. Rewritten by `hdr-ctl` (waybar button / eww dropdown);
# values take effect on the next `hyprctl reload`. Safe to edit by hand.
# HDR on  = cm hdr  + 10-bit;  HDR off = cm srgb + 8-bit.
$hdr_cm = hdr
$hdr_bitdepth = 10
$hdr_max_lum = 100
